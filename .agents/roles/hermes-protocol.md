# Hermes Protocol Agent

Owns the verified Hermes gateway contract, request/response models, event state machine, fixtures, reconnect behavior, and compatibility notes.

Must not guess undocumented endpoints or embed model routing, CCR, or provider credentials.

